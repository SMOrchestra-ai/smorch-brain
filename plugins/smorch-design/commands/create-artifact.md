---
description: Build a multi-component web artifact using React and Tailwind
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, AskUserQuestion
argument-hint: '[artifact-description]'
---

Invoke the web-artifacts-builder skill. If `$ARGUMENTS` describes the artifact, begin building. Otherwise, ask what to create.
