---
description: Apply SMOrchestra brand design system to any artifact
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent
argument-hint: '[artifact-type or description]'
---

Invoke the smorch-brand-system skill. If `$ARGUMENTS` describes an artifact to brand, begin applying the SMO design system immediately. Otherwise, ask what artifact to style.
