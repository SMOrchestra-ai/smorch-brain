---
description: Build or debug an n8n workflow
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, AskUserQuestion
argument-hint: '[workflow-name or debug]'
---

Invoke the n8n-architect skill to design, build, deploy, debug, or optimize an n8n workflow.

If `$ARGUMENTS` contains a workflow name, use it as context. If "debug", enter debugging mode for an existing workflow. Otherwise, ask the user what they need.
