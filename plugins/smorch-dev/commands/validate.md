---
description: Run a rapid validation sprint on an idea or product
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, WebSearch, WebFetch
argument-hint: '[idea-or-product]'
---

Invoke the validation-sprint skill. If `$ARGUMENTS` describes an idea or product, begin the validation sprint immediately. Otherwise, ask the user what they want to validate.
