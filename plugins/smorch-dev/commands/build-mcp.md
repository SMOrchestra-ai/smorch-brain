---
description: Build an MCP server for a third-party API or service
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent, WebFetch, WebSearch
argument-hint: '[api-or-service-name]'
---

Invoke the mcp-builder skill to create an MCP server. If `$ARGUMENTS` provides a service name, research its API and begin building. Otherwise, ask what service to integrate.
