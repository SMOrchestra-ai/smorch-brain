---
description: Test a local web application using Playwright
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Agent
argument-hint: '[url-or-description]'
---

Invoke the webapp-testing skill. If `$ARGUMENTS` contains a URL or app description, begin testing immediately. Otherwise, ask the user for the local URL to test.
